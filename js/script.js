var $cardFotos = $("div.cardFotos");
var caminho="../cheiadegraca/img/biquini";
var links=[];
 console.log(links)

for( var i=0; i<$cardFotos.length; i++){
    links += caminho + $cardFotos[i]++;
    console.log(links)
   
}